services:
  app:
    build:
      context: .
      dockerfile: .devcontainer/Dockerfile
    command: npm run dev -- --host
    ports:
      - "5000:5000"
    volumes:
      - .:/app
      - /app/node_modules
