---
description: Start the Vite dev server in Docker (foreground, hot-reload)
---

Run `docker compose up --build` in the foreground to start the dev server with hot-reload. The app will be at http://localhost:5000. If port 5000 is busy, suggest `/kill5000` first.
