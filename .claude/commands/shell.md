---
description: Open a shell inside the running app container
---

Run `docker compose exec app sh` to drop into a shell inside the running container. If no container is running, suggest starting `/dev` first.
