---
description: Free port 5000 by killing whatever process is bound to it
---

Run `lsof -ti:5000 | xargs kill` to free port 5000. Use this when `docker compose up` fails with "port is already allocated". If `lsof` returns nothing, no action is needed.
